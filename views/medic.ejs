<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Medical History</title>
<style>
/* Modern Futuristic Doctors List CSS */

:root {
    --primary: #2563eb;
    --primary-dark: #1d4ed8;
    --primary-light: #3b82f6;
    --accent: #06b6d4;
    --success: #10b981;
    --danger: #ef4444;
    --danger-dark: #dc2626;
    --warning: #f59e0b;
    --background: #f8fafc;
    --card: #ffffff;
    --text: #1e293b;
    --text-light: #64748b;
    --border: #e2e8f0;
    --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
    --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    --radius: 8px;
    --transition: all 0.2s ease;
  }
  
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Inter', 'Segoe UI', system-ui, -apple-system, sans-serif;
  }
  
  body {
    background-color: var(--background);
    color: var(--text);
    line-height: 1.6;
    padding: 2rem;
    max-width: 1400px;
    margin: 0 auto;
    background-image: 
      radial-gradient(circle at 20% 30%, rgba(37, 99, 235, 0.03) 0%, transparent 25%),
      radial-gradient(circle at 80% 70%, rgba(6, 182, 212, 0.03) 0%, transparent 30%);
  }
  
  h1 {
    font-size: 2.25rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    color: var(--primary);
    position: relative;
    padding-bottom: 0.75rem;
  }
  
  h1::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 80px;
    height: 4px;
    background: linear-gradient(90deg, var(--primary), var(--accent));
    border-radius: 2px;
  }
  
  /* Search Form Styling */
  .search-form {
    background: var(--card);
    padding: 1.5rem;
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    margin-bottom: 2rem;
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    align-items: flex-end;
    position: relative;
    border: 1px solid var(--border);
  }
  
  .search-form::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 4px;
    background: linear-gradient(90deg, var(--primary), var(--accent));
    border-radius: var(--radius) var(--radius) 0 0;
  }
  
  .search-input {
    flex: 1;
    min-width: 150px;
    padding: 0.75rem 1rem;
    border: 1px solid var(--border);
    border-radius: var(--radius);
    font-size: 0.95rem;
    transition: var(--transition);
    background-color: #f8fafc;
    color: var(--text);
  }
  
  .search-input:focus {
    outline: none;
    border-color: var(--primary-light);
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.2);
  }
  
  .search-input::placeholder {
    color: var(--text-light);
  }
  
  .search-button {
    background: linear-gradient(135deg, var(--primary), var(--primary-dark));
    color: white;
    border: none;
    padding: 0.75rem 1.5rem;
    border-radius: var(--radius);
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
    box-shadow: var(--shadow-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 120px;
  }
  
  .search-button:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow);
    background: linear-gradient(135deg, var(--primary-light), var(--primary));
  }
  
  .search-button:active {
    transform: translateY(0);
  }
  
  /* Table Styling */
  table {
    width: 100%;
    border-collapse: separate;
    border-spacing: 0;
    background: var(--card);
    border-radius: var(--radius);
    overflow: hidden;
    box-shadow: var(--shadow);
    margin-bottom: 2rem;
    border: 1px solid var(--border);
  }
  
  thead {
    background: linear-gradient(to right, #f1f5f9, #f8fafc);
  }
  
  th {
    text-transform: uppercase;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    color: var(--text-light);
    padding: 1rem;
    text-align: left;
    border-bottom: 2px solid var(--border);
    position: relative;
  }
  
  th:after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, var(--primary-light), transparent);
    opacity: 0.3;
  }
  
  td {
    padding: 1rem;
    border-bottom: 1px solid var(--border);
    font-size: 0.95rem;
    transition: var(--transition);
  }
  
  tr:last-child td {
    border-bottom: none;
  }
  
  tbody tr {
    transition: var(--transition);
  }
  
  tbody tr:hover {
    background-color: rgba(59, 130, 246, 0.05);
  }
  
  /* Delete Button Styling */
  .delete-btn {
    background-color: var(--danger);
    color: white;
    border: none;
    padding: 0.5rem 1rem;
    border-radius: var(--radius);
    font-size: 0.85rem;
    font-weight: 500;
    cursor: pointer;
    transition: var(--transition);
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }
  
  .delete-btn:hover {
    background-color: var(--danger-dark);
    transform: translateY(-1px);
    box-shadow: 0 2px 4px rgba(239, 68, 68, 0.2);
  }
  
  .delete-btn:active {
    transform: translateY(0);
  }
  
  /* Empty State Styling */
  tbody tr td[colspan="9"] {
    padding: 3rem 1rem;
    text-align: center;
    color: var(--text-light);
    font-style: italic;
  }
  
  /* Add some icons to enhance the UI */
  .search-button::before {
    content: '🔍';
    margin-right: 0.5rem;
  }
  
  .delete-btn::before {
    content: '🗑️';
    margin-right: 0.25rem;
    font-size: 0.85rem;
  }
  
  /* Responsive adjustments */
  @media (max-width: 1200px) {
    body {
      padding: 1.5rem;
    }
    
    .search-form {
      padding: 1.25rem;
    }
    
    table {
      display: block;
      overflow-x: auto;
      white-space: nowrap;
    }
  }
  
  @media (max-width: 768px) {
    body {
      padding: 1rem;
    }
    
    h1 {
      font-size: 1.75rem;
    }
    
    .search-form {
      padding: 1rem;
      flex-direction: column;
      align-items: stretch;
    }
    
    .search-input {
      width: 100%;
    }
    
    .search-button {
      width: 100%;
    }
    
    th, td {
      padding: 0.75rem;
    }
  }
  
  /* Add a subtle animation for the table rows */
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }
  
  tbody tr {
    animation: fadeIn 0.3s ease-out forwards;
  }
  
  tbody tr:nth-child(1) { animation-delay: 0.05s; }
  tbody tr:nth-child(2) { animation-delay: 0.1s; }
  tbody tr:nth-child(3) { animation-delay: 0.15s; }
  tbody tr:nth-child(4) { animation-delay: 0.2s; }
  tbody tr:nth-child(5) { animation-delay: 0.25s; }
  tbody tr:nth-child(6) { animation-delay: 0.3s; }
  tbody tr:nth-child(7) { animation-delay: 0.35s; }
  tbody tr:nth-child(8) { animation-delay: 0.4s; }
  tbody tr:nth-child(9) { animation-delay: 0.45s; }
  tbody tr:nth-child(10) { animation-delay: 0.5s; }
  
  /* Add a cool highlight effect for the doctor ID column */
  td:first-child {
    font-weight: 600;
    color: var(--primary);
  }
  
  /* Add a badge style for employment type */
  td:nth-child(6) {
    position: relative;
  }
  
  td:nth-child(6)::after {
    content: attr(data-value);
    position: absolute;
    top: 50%;
    left: 1rem;
    transform: translateY(-50%);
    padding: 0.25rem 0.5rem;
    border-radius: 9999px;
    font-size: 0.75rem;
    font-weight: 500;
    background-color: #e0f2fe;
    color: #0369a1;
  }
  
  /* Add a badge style for specialization */
  td:nth-child(8) {
    position: relative;
  }
  
  td:nth-child(8)::after {
    content: attr(data-value);
    position: absolute;
    top: 50%;
    left: 1rem;
    transform: translateY(-50%);
    padding: 0.25rem 0.5rem;
    border-radius: 9999px;
    font-size: 0.75rem;
    font-weight: 500;
    background-color: #f0fdf4;
    color: #166534;
  }
  
  /* Add a subtle hover effect to the search inputs */
  .search-input:hover {
    border-color: var(--primary-light);
  }
</style>
</head>
<body>
<h1>Medical History</h1>

<!-- Advanced Search Form -->
<form class="search-form" action="/medic" method="GET">
  <input type="text" name="RecordID" class="search-input" placeholder="Record ID" value="<%= query?.RecordID || '' %>">
  <input type="text" name="PatientID" class="search-input" placeholder="Patient ID" value="<%= query?.PatientID || '' %>">
  <input type="text" name="DoctorID" class="search-input" placeholder="Doctor ID" value="<%= query?.DoctorID || '' %>">
  <input type="text" name="Disease" class="search-input" placeholder="Disease" value="<%= query?.Disease || '' %>">
  <input type="text" name="OriginalWard" class="search-input" placeholder="Original Ward" value="<%= query?.OriginalWard || '' %>">
  <input type="text" name="DischargeWard" class="search-input" placeholder="Discharge Ward" value="<%= query?.DischargeWard || '' %>">
  <button type="submit" class="search-button">Search</button>
</form>

<table>
<thead>
  <tr>
    <th>Record ID</th>
    <th>Patient ID</th>
    <th>Doctor ID</th>
    <th>Disease</th>
    <th>Original Ward</th>
    <th>Discharge Ward</th>
    <th>Action</th>
  </tr>
</thead>
<tbody>
  <% if (records.length > 0) { %>
    <% records.forEach(record => { %>
      <tr>
        <td><%= record.RecordID %></td>
        <td><%= record.PatientID %></td>
        <td><%= record.DoctorID %></td>
        <td><%= record.Disease %></td>
        <td><%= record.OriginalWard %></td>
        <td><%= record.DischargeWard %></td>
        <td>
          <!-- Form to send a POST request to delete the record -->
          <form action="/delete-record" method="POST" style="display:inline;">
            <input type="hidden" name="recordId" value="<%= record.RecordID %>">
            <button type="submit" class="delete-btn">Delete</button>
          </form>
        </td>
      </tr>
    <% }); %>
  <% } else { %>
    <tr>
      <td colspan="7" style="text-align: center;">No records found.</td>
    </tr>
  <% } %>
</tbody>
</table>
</body>
</html>