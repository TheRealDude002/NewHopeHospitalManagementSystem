<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Doctors List</title>
    <style>
        /* Modern Futuristic Doctors List CSS */

:root {
    --primary: #2563eb;
    --primary-dark: #1d4ed8;
    --primary-light: #3b82f6;
    --accent: #06b6d4;
    --success: #10b981;
    --danger: #ef4444;
    --danger-dark: #dc2626;
    --warning: #f59e0b;
    --background: #f8fafc;
    --card: #ffffff;
    --text: #1e293b;
    --text-light: #64748b;
    --border: #e2e8f0;
    --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
    --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    --radius: 8px;
    --transition: all 0.2s ease;
  }
  
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Inter', 'Segoe UI', system-ui, -apple-system, sans-serif;
  }
  
  body {
    background-color: var(--background);
    color: var(--text);
    line-height: 1.6;
    padding: 0;
    max-width: 1400px;
    margin: 0 auto;
    background-image: 
      radial-gradient(circle at 20% 30%, rgba(37, 99, 235, 0.03) 0%, transparent 25%),
      radial-gradient(circle at 80% 70%, rgba(6, 182, 212, 0.03) 0%, transparent 30%);
  }

  /* Navigation Bar Styling */
  .navbar {
    background: var(--card);
    box-shadow: var(--shadow);
    padding: 0.75rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: sticky;
    top: 0;
    z-index: 100;
    border-bottom: 1px solid var(--border);
  }

  .navbar::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 3px;
    background: linear-gradient(90deg, var(--primary), var(--accent));
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    text-decoration: none;
  }

  .logo-icon {
    font-size: 1.5rem;
    color: var(--primary);
    background: linear-gradient(135deg, var(--primary), var(--accent));
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .logo-text {
    font-weight: 700;
    font-size: 1.25rem;
    color: var(--text);
    letter-spacing: -0.5px;
  }

  .nav-links {
    display: flex;
    gap: 1.5rem;
    list-style: none;
  }

  .nav-link {
    text-decoration: none;
    color: var(--text);
    font-weight: 500;
    font-size: 0.95rem;
    padding: 0.5rem 0;
    position: relative;
    transition: var(--transition);
  }

  .nav-link:hover {
    color: var(--primary);
  }

  .nav-link::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background: linear-gradient(90deg, var(--primary), var(--accent));
    transition: var(--transition);
    border-radius: 2px;
  }

  .nav-link:hover::after {
    width: 100%;
  }

  .nav-link.active {
    color: var(--primary);
  }

  .nav-link.active::after {
    width: 100%;
  }

  .nav-actions {
    display: flex;
    gap: 1rem;
    align-items: center;
  }

  .nav-button {
    background: transparent;
    color: var(--text);
    border: 1px solid var(--border);
    padding: 0.5rem 1rem;
    border-radius: var(--radius);
    font-weight: 500;
    font-size: 0.9rem;
    cursor: pointer;
    transition: var(--transition);
  }

  .nav-button:hover {
    border-color: var(--primary-light);
    color: var(--primary);
  }

  .nav-button.primary {
    background: linear-gradient(135deg, var(--primary), var(--primary-dark));
    color: white;
    border: none;
  }

  .nav-button.primary:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow);
    background: linear-gradient(135deg, var(--primary-light), var(--primary));
  }

  .mobile-menu-btn {
    display: none;
    background: none;
    border: none;
    font-size: 1.5rem;
    color: var(--text);
    cursor: pointer;
  }

  /* Content area padding */
  .content {
    padding: 2rem;
  }

  /* Responsive adjustments for navbar */
  @media (max-width: 900px) {
    .nav-links {
      display: none;
    }

    .mobile-menu-btn {
      display: block;
    }

    .nav-actions {
      display: none;
    }

    .navbar {
      padding: 0.75rem 1rem;
    }

    .mobile-menu {
      position: fixed;
      top: 0;
      right: -100%;
      width: 70%;
      max-width: 300px;
      height: 100vh;
      background: var(--card);
      box-shadow: var(--shadow-lg);
      transition: var(--transition);
      z-index: 200;
      padding: 2rem;
      display: flex;
      flex-direction: column;
      gap: 2rem;
    }

    .mobile-menu.active {
      right: 0;
    }

    .mobile-menu .nav-links {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .mobile-menu .nav-actions {
      display: flex;
      flex-direction: column;
      width: 100%;
    }

    .mobile-menu-close {
      position: absolute;
      top: 1rem;
      right: 1rem;
      background: none;
      border: none;
      font-size: 1.5rem;
      color: var(--text);
      cursor: pointer;
    }

    .overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      z-index: 199;
      opacity: 0;
      pointer-events: none;
      transition: var(--transition);
    }

    .overlay.active {
      opacity: 1;
      pointer-events: all;
    }
  }
  
  h1 {
    font-size: 2.25rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    color: var(--primary);
    position: relative;
    padding-bottom: 0.75rem;
  }
  
  h1::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 80px;
    height: 4px;
    background: linear-gradient(90deg, var(--primary), var(--accent));
    border-radius: 2px;
  }
  
  /* Search Form Styling */
  .search-form {
    background: var(--card);
    padding: 1.5rem;
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    margin-bottom: 2rem;
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    align-items: flex-end;
    position: relative;
    border: 1px solid var(--border);
  }
  
  .search-form::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 4px;
    background: linear-gradient(90deg, var(--primary), var(--accent));
    border-radius: var(--radius) var(--radius) 0 0;
  }
  
  .search-input {
    flex: 1;
    min-width: 150px;
    padding: 0.75rem 1rem;
    border: 1px solid var(--border);
    border-radius: var(--radius);
    font-size: 0.95rem;
    transition: var(--transition);
    background-color: #f8fafc;
    color: var(--text);
  }
  
  .search-input:focus {
    outline: none;
    border-color: var(--primary-light);
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.2);
  }
  
  .search-input::placeholder {
    color: var(--text-light);
  }
  
  .search-button {
    background: linear-gradient(135deg, var(--primary), var(--primary-dark));
    color: white;
    border: none;
    padding: 0.75rem 1.5rem;
    border-radius: var(--radius);
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
    box-shadow: var(--shadow-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 120px;
  }
  
  .search-button:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow);
    background: linear-gradient(135deg, var(--primary-light), var(--primary));
  }
  
  .search-button:active {
    transform: translateY(0);
  }
  
  /* Table Styling */
  table {
    width: 100%;
    border-collapse: separate;
    border-spacing: 0;
    background: var(--card);
    border-radius: var(--radius);
    overflow: hidden;
    box-shadow: var(--shadow);
    margin-bottom: 2rem;
    border: 1px solid var(--border);
  }
  
  thead {
    background: linear-gradient(to right, #f1f5f9, #f8fafc);
  }
  
  th {
    text-transform: uppercase;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    color: var(--text-light);
    padding: 1rem;
    text-align: left;
    border-bottom: 2px solid var(--border);
    position: relative;
  }
  
  th:after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, var(--primary-light), transparent);
    opacity: 0.3;
  }
  
  td {
    padding: 1rem;
    border-bottom: 1px solid var(--border);
    font-size: 0.95rem;
    transition: var(--transition);
  }
  
  tr:last-child td {
    border-bottom: none;
  }
  
  tbody tr {
    transition: var(--transition);
  }
  
  tbody tr:hover {
    background-color: rgba(59, 130, 246, 0.05);
  }
  
  /* Delete Button Styling */
  .delete-btn {
    background-color: var(--danger);
    color: white;
    border: none;
    padding: 0.5rem 1rem;
    border-radius: var(--radius);
    font-size: 0.85rem;
    font-weight: 500;
    cursor: pointer;
    transition: var(--transition);
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }
  
  .delete-btn:hover {
    background-color: var(--danger-dark);
    transform: translateY(-1px);
    box-shadow: 0 2px 4px rgba(239, 68, 68, 0.2);
  }
  
  .delete-btn:active {
    transform: translateY(0);
  }
  
  /* Empty State Styling */
  tbody tr td[colspan="9"] {
    padding: 3rem 1rem;
    text-align: center;
    color: var(--text-light);
    font-style: italic;
  }
  
  /* Add some icons to enhance the UI */
  .search-button::before {
    content: '🔍';
    margin-right: 0.5rem;
  }
  
  .delete-btn::before {
    content: '🗑️';
    margin-right: 0.25rem;
    font-size: 0.85rem;
  }
  
  /* Responsive adjustments */
  @media (max-width: 1200px) {
    .content {
      padding: 1.5rem;
    }
    
    .search-form {
      padding: 1.25rem;
    }
    
    table {
      display: block;
      overflow-x: auto;
      white-space: nowrap;
    }
  }
  
  @media (max-width: 768px) {
    .content {
      padding: 1rem;
    }
    
    h1 {
      font-size: 1.75rem;
    }
    
    .search-form {
      padding: 1rem;
      flex-direction: column;
      align-items: stretch;
    }
    
    .search-input {
      width: 100%;
    }
    
    .search-button {
      width: 100%;
    }
    
    th, td {
      padding: 0.75rem;
    }
  }
  
  /* Add a subtle animation for the table rows */
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }
  
  tbody tr {
    animation: fadeIn 0.3s ease-out forwards;
  }
  
  tbody tr:nth-child(1) { animation-delay: 0.05s; }
  tbody tr:nth-child(2) { animation-delay: 0.1s; }
  tbody tr:nth-child(3) { animation-delay: 0.15s; }
  tbody tr:nth-child(4) { animation-delay: 0.2s; }
  tbody tr:nth-child(5) { animation-delay: 0.25s; }
  tbody tr:nth-child(6) { animation-delay: 0.3s; }
  tbody tr:nth-child(7) { animation-delay: 0.35s; }
  tbody tr:nth-child(8) { animation-delay: 0.4s; }
  tbody tr:nth-child(9) { animation-delay: 0.45s; }
  tbody tr:nth-child(10) { animation-delay: 0.5s; }
  
  /* Add a cool highlight effect for the doctor ID column */
  td:first-child {
    font-weight: 600;
    color: var(--primary);
  }
  
  /* Add a badge style for employment type */
  td:nth-child(6) {
    position: relative;
  }
  
  td:nth-child(6)::after {
    content: attr(data-value);
    position: absolute;
    top: 50%;
    left: 1rem;
    transform: translateY(-50%);
    padding: 0.25rem 0.5rem;
    border-radius: 9999px;
    font-size: 0.75rem;
    font-weight: 500;
    background-color: #e0f2fe;
    color: #0369a1;
  }
  
  /* Add a badge style for specialization */
  td:nth-child(8) {
    position: relative;
  }
  
  td:nth-child(8)::after {
    content: attr(data-value);
    position: absolute;
    top: 50%;
    left: 1rem;
    transform: translateY(-50%);
    padding: 0.25rem 0.5rem;
    border-radius: 9999px;
    font-size: 0.75rem;
    font-weight: 500;
    background-color: #f0fdf4;
    color: #166534;
  }
  
  /* Add a subtle hover effect to the search inputs */
  .search-input:hover {
    border-color: var(--primary-light);
  }
    </style>
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar">
        <ul class="nav-links">
            <li><a href="/" class="nav-link active">Doctors</a></li>
            <li><a href="/patientview.html" class="nav-link">Patients</a></li>
            <li><a href="/wardview.html" class="nav-link">Wards</a></li>
            <li><a href="/payment.html" class="nav-link">Payment</a></li>
            <li><a href="/medicview.html" class="nav-link">Medical</a></li>
        </ul>  
    </nav>
    <div class="overlay"></div>
    
    <!-- Main Content -->
    <div class="content">
        <h1>Doctors List</h1>

        <!-- Advanced Search Form -->
        <form class="search-form" action="/doctors" method="GET">
            <input type="text" name="DoctorID" class="search-input" placeholder="Doctor ID" value="<%= query?.DoctorID || '' %>">
            <input type="text" name="FirstName" class="search-input" placeholder="First Name" value="<%= query?.FirstName || '' %>">
            <input type="text" name="LastName" class="search-input" placeholder="Last Name" value="<%= query?.LastName || '' %>">
            <input type="text" name="Address" class="search-input" placeholder="Address" value="<%= query?.Address || '' %>">
            <input type="text" name="Ward_Id" class="search-input" placeholder="Ward ID" value="<%= query?.Ward_ID || '' %>">
            <button type="submit" class="search-button">Search</button>
        </form>

        <table>
            <thead>
                <tr>
                    <th>Doctor ID</th>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Address</th>
                    <th>Phone Number</th>
                    <th>Employment Type</th>
                    <th>Ward ID</th>
                    <th>Specialization</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% if (doctors.length > 0) { %>
                    <% doctors.forEach(doctor => { %>
                        <tr>
                            <td><%= doctor.DoctorID %></td>
                            <td><%= doctor.FirstName %></td>
                            <td><%= doctor.LastName %></td>
                            <td><%= doctor.Address %></td>
                            <td><%= doctor.Phone_Num %></td>
                            <td><%= doctor.Employment_Type %></td>
                            <td><%= doctor.Ward_Id %></td>
                            <td><%= doctor.Specialization %></td>
                            <td>
                                <!-- Form to send a POST request to delete the doctor -->
                                <form action="/delete-doctor" method="POST" style="display:inline;">
                                    <input type="hidden" name="DoctorID" value="<%= doctor.DoctorID %>">
                                    <button type="submit" class="delete-btn">Delete</button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="9" style="text-align: center;">No doctors found.</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>

    <!-- JavaScript for mobile menu toggle -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const mobileMenuBtn = document.querySelector('.mobile-menu-btn');
            const mobileMenuClose = document.querySelector('.mobile-menu-close');
            const mobileMenu = document.querySelector('.mobile-menu');
            const overlay = document.querySelector('.overlay');
            
            mobileMenuBtn.addEventListener('click', function() {
                mobileMenu.classList.add('active');
                overlay.classList.add('active');
                document.body.style.overflow = 'hidden';
            });
            
            mobileMenuClose.addEventListener('click', function() {
                mobileMenu.classList.remove('active');
                overlay.classList.remove('active');
                document.body.style.overflow = '';
            });
            
            overlay.addEventListener('click', function() {
                mobileMenu.classList.remove('active');
                overlay.classList.remove('active');
                document.body.style.overflow = '';
            });
        });
    </script>
</body>
</html>

